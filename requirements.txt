# Prototipo de anotación clínica con LLM (TDAH / BRIEF-2)
# Python 3.12 · entorno: .venv

pandas>=2.2             # lectura de la BD y tablas de resultados
matplotlib>=3.8         # gráficos del cuaderno de comparación
pydantic>=2.10          # el contrato de salida (Anotacion / ItemDetectado)
langchain-ollama>=1.1   # backend de salida estructurada (json_schema)

ipykernel>=7.0          # ejecutar los cuadernos
